{
	"email": "{{email}}",
	"password": "{{password}}",
    "workspaces": [
        {
            "name": "{{workspace name 1}}",
            "id": "{{X-Workspace-Id 1}}",
            "gwapi-key": "{{X-Api-Key 1}}",
            "access_key_id": "{{access_key_id of the public bucket in the chosen workspace}}",
            "secret_access_key": "{{secret_access_key of the public bucket in the chosen workspace}}",
            "endpoint_url": "{{endpoint_url of the public bucket in the chosen workspace}}",
            "bucket_name": "{{bucket_name of the public bucket in the chosen workspace}}"
        },
        {
            "name": "{{workspace name 2}}",
            "id": "{{X-Workspace-Id 2}}",
            "gwapi-key": "{{X-Api-Key 2}}",
            "access_key_id": "{{access_key_id of the public bucket in the chosen workspace}}",
            "secret_access_key": "{{secret_access_key of the public bucket in the chosen workspace}}",
            "endpoint_url": "{{endpoint_url of the public bucket in the chosen workspace}}",
            "bucket_name": "{{bucket_name of the public bucket in the chosen workspace}}"
        },
        {
            "name": "{{workspace name 3}}",
            "id": "{{X-Workspace-Id 3}}",
            "gwapi-key": "{{X-Api-Key 3}}",
            "access_key_id": "{{access_key_id of the public bucket in the chosen workspace}}",
            "secret_access_key": "{{secret_access_key of the public bucket in the chosen workspace}}",
            "endpoint_url": "{{endpoint_url of the public bucket in the chosen workspace}}",
            "bucket_name": "{{bucket_name of the public bucket in the chosen workspace}}"
        },
        {
            "name": "{{workspace name N}}",
            "id": "{{X-Workspace-Id N}}",
            "gwapi-key": "{{X-Api-Key N}}",
            "access_key_id": "{{access_key_id of the public bucket in the chosen workspace}}",
            "secret_access_key": "{{secret_access_key of the public bucket in the chosen workspace}}",
            "endpoint_url": "{{endpoint_url of the public bucket in the chosen workspace}}",
            "bucket_name": "{{bucket_name of the public bucket in the chosen workspace}}"
        }
    ]
}